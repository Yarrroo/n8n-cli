# n8n-cli — Core

Python CLI making n8n workflows AI-manageable: node/connection/pin-data/folder/execution ops, summarized execution data (≤1 KB for 2 MB payload), multi-instance auth. n8n is always source of truth — no local workflow cache.

**Authoritative reference**: CLAUDE.md (always-loaded; covers resource model, dual-API reality, architecture rules, output contract, anti-patterns).

## Memory graph

- Stack and build tooling: `mem:tech_stack`
- Developer commands (run, test, lint): `mem:suggested_commands`
- Code topology (composition edges, delegation chains): `mem:mind_map`
- Conventions are fully stated in CLAUDE.md — no separate memory needed.
